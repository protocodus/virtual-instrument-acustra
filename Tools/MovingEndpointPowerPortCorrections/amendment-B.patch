--- MovingPortAudit-A.cpp
+++ MovingPortAudit-B.cpp
@@ -1,4 +1,4 @@
-// Protocol: protocol-v1.md + protocol-A-v2.md. Independently derived 13-coordinate numerical audit.
+// Protocol: protocol-v1.md + protocol-A-v2.md + protocol-B.md. Independently derived 13-coordinate numerical audit.
 #define main frozen_prototype_main
 #include "frozen-prototype.cpp"
 #undef main
@@ -110,10 +110,28 @@
 };
 
 struct TrialResult{Error error;double joint=0,stringBalance=0,bodyBalance=0,forceMismatch=0,forcePeak=0;};
-TrialResult trajectory(experiment::Audit&a,const Model&m,const Case&c,const Reference*ref,int rate,double lambda,bool naive=false){Sav s(m,rate,lambda,naive);s.initialise(c.initial);double es0=s.stringEnergy(),eb0=s.bodyEnergy(),e0=es0+eb0,win=0,winAbs=0,wps=0,wpb=0,wds=0,wdb=0,wsAbs=0,wbAbs=0;Gap aux,mass,space;TrialResult result;int frames=static_cast<int>(rate*c.seconds),ratio=rate/48000;for(int n=0;n<frames;++n){double t=double(n)/rate,force=c.driven?pulse(t):0,eta=c.driven?(n>=rate/5?100:1):0;auto f=s.step(force,eta,c.driven);requireObservationFinite(s,f);double wi=s.h*force*dot(m.b,f.v),ps=s.h*f.stringForce*f.v[Q],pb=s.h*f.bodyForce*f.v[Q],ds=s.h*quad(m.damping(eta,false),f.v),db=s.h*(c.driven?m.cb:0)*f.v[Q]*f.v[Q];win+=wi;winAbs+=std::abs(wi);wps+=ps;wpb+=pb;wds+=ds;wdb+=db;wsAbs+=std::abs(wi)+std::abs(ps)+ds;wbAbs+=std::abs(pb)+db;double es=s.stringEnergy(),eb=s.bodyEnergy(),norm=std::max(std::abs(e0)+winAbs,1e-18);requireFinite({win,winAbs,wps,wpb,wds,wdb,wsAbs,wbAbs,es,eb,norm},"nonfinite trajectory work");result.joint=std::max(result.joint,std::abs(es+eb-e0-win+wds+wdb)/norm);result.stringBalance=std::max(result.stringBalance,std::abs(es-es0-win+wps+wds)/std::max(std::abs(es0)+wsAbs,1e-18));result.bodyBalance=std::max(result.bodyBalance,std::abs(eb-eb0-wpb+wdb)/std::max(std::abs(eb0)+wbAbs,1e-18));result.forceMismatch=std::max(result.forceMismatch,std::abs(f.bodyForce-f.stringForce));result.forcePeak=std::max({result.forcePeak,std::abs(f.bodyForce),std::abs(f.stringForce)});if(n%ratio==0){if(ref)result.error.add(f,ref->fine[n/ratio]);aux.add(f.bodyForce,f.directNumerical);mass.add(f.directNumerical,f.physicalAtState);if(ref)space.add(ref->fine[n/ratio].bodyForce,ref->fine[n/ratio].point);}if(!std::isfinite(es+eb+s.psi))throw std::runtime_error("nonfinite candidate trajectory");}
+TrialResult trajectory(experiment::Audit&a,const Model&m,const Case&c,const Reference*ref,int rate,double lambda,bool naive=false){Sav s(m,rate,lambda,naive);s.initialise(c.initial);double es0=s.stringEnergy(),eb0=s.bodyEnergy(),e0=es0+eb0,win=0,winAbs=0,wps=0,wpb=0,wds=0,wdb=0,wsAbs=0,wbAbs=0;Gap aux,mass,space,dampingTiming;std::string jumpObservation="null";double jumpSquaredForceError=0;TrialResult result;int frames=static_cast<int>(rate*c.seconds),ratio=rate/48000;for(int n=0;n<frames;++n){double t=double(n)/rate,force=c.driven?pulse(t):0,instantEta=c.driven?(n>=rate/5?100:1):0;
+ // B: only the centered cell containing the exact t=.2 damping jump changes.
+ // Physical RK4 and its right-sided observation remain untouched.
+ double eta=c.driven&&n==rate/5?50.5:instantEta;
+ auto f=s.step(force,eta,c.driven);requireObservationFinite(s,f);double wi=s.h*force*dot(m.b,f.v),ps=s.h*f.stringForce*f.v[Q],pb=s.h*f.bodyForce*f.v[Q],ds=s.h*quad(m.damping(eta,false),f.v),db=s.h*(c.driven?m.cb:0)*f.v[Q]*f.v[Q];win+=wi;winAbs+=std::abs(wi);wps+=ps;wpb+=pb;wds+=ds;wdb+=db;wsAbs+=std::abs(wi)+std::abs(ps)+ds;wbAbs+=std::abs(pb)+db;double es=s.stringEnergy(),eb=s.bodyEnergy(),norm=std::max(std::abs(e0)+winAbs,1e-18);requireFinite({win,winAbs,wps,wpb,wds,wdb,wsAbs,wbAbs,es,eb,norm},"nonfinite trajectory work");result.joint=std::max(result.joint,std::abs(es+eb-e0-win+wds+wdb)/norm);result.stringBalance=std::max(result.stringBalance,std::abs(es-es0-win+wps+wds)/std::max(std::abs(es0)+wsAbs,1e-18));result.bodyBalance=std::max(result.bodyBalance,std::abs(eb-eb0-wpb+wdb)/std::max(std::abs(eb0)+wbAbs,1e-18));result.forceMismatch=std::max(result.forceMismatch,std::abs(f.bodyForce-f.stringForce));result.forcePeak=std::max({result.forcePeak,std::abs(f.bodyForce),std::abs(f.stringForce)});if(n%ratio==0){if(ref)result.error.add(f,ref->fine[n/ratio]);aux.add(f.bodyForce,f.directNumerical);mass.add(f.directNumerical,f.physicalAtState);
+ double instantaneousPhysicalForce=f.physicalAtState;
+ if(eta!=instantEta){
+  auto instantaneousAcceleration=m.acceleration(f.z,f.v,force,m.damping(instantEta,c.driven),m.Minv);
+  instantaneousPhysicalForce=m.mb*instantaneousAcceleration[Q]+(c.driven?m.cb:0)*f.v[Q]+m.kb*f.z[Q];
+  for(double value:instantaneousAcceleration)requireFinite({value},"nonfinite instantaneous physical diagnostic acceleration");
+ }
+ requireFinite({instantaneousPhysicalForce},"nonfinite instantaneous physical diagnostic force");
+ dampingTiming.add(f.physicalAtState,instantaneousPhysicalForce);
+ if(c.driven&&n==rate/5){
+  if(ref)jumpSquaredForceError=std::pow(f.bodyForce-ref->fine[n/ratio].bodyForce,2);
+  requireFinite({jumpSquaredForceError},"nonfinite jump force diagnostic");
+  jumpObservation="{\"time_s\":"+num(t)+",\"candidate_eta\":"+num(eta)+",\"physical_instantaneous_eta\":"+num(instantEta)+",\"sav_body_force_n\":"+num(f.bodyForce)+",\"sav_string_force_n\":"+num(f.stringForce)+",\"direct_numerical_cell_force_n\":"+num(f.directNumerical)+",\"physical_mass_cell_force_at_candidate_state_n\":"+num(f.physicalAtState)+",\"physical_mass_instantaneous_force_at_candidate_state_n\":"+num(instantaneousPhysicalForce)+",\"reference_right_sided_body_force_n\":"+(ref?num(ref->fine[n/ratio].bodyForce):"null")+"}";
+ }
+ if(ref)space.add(ref->fine[n/ratio].bodyForce,ref->fine[n/ratio].point);}if(!std::isfinite(es+eb+s.psi))throw std::runtime_error("nonfinite candidate trajectory");}
  a.check(result.joint<1e-8&&result.stringBalance<1e-8&&result.bodyBalance<1e-8,"joint or branch power ledger gate");a.check(result.forceMismatch/std::max(result.forcePeak,1e-30)<1e-10,"independent branch reaction gate");a.check(s.minPsi>=0&&s.minDen>=1,"auxiliary sign or rank denominator gate");
  Sav observation=s;auto end=observation.step(c.driven?pulse(c.seconds):0,c.driven?100:0,c.driven);
- a.add("\"test\":\"trajectory\",\"case\":\""+c.name+"\",\"rate\":"+std::to_string(rate)+",\"lambda\":"+num(lambda)+",\"naive_control\":"+(naive?"true":"false")+","+(ref?result.error.fields():"\"q_error\":null,\"v_error\":null,\"y_error\":null,\"vy_error\":null,\"port_force_error\":null")+",\"joint_balance\":"+num(result.joint)+",\"string_balance\":"+num(result.stringBalance)+",\"body_balance\":"+num(result.bodyBalance)+",\"port_branch_peak_error_n\":"+num(result.forceMismatch)+",\"port_force_peak_n\":"+num(result.forcePeak)+",\"minimum_psi\":"+num(s.minPsi)+",\"minimum_denominator\":"+num(s.minDen)+",\"maximum_denominator\":"+num(s.maxDen)+",\"maximum_phi_drift_j\":"+num(s.maxDrift)+",\"input_work_j\":"+num(win)+",\"string_port_work_j\":"+num(wps)+",\"body_port_work_j\":"+num(wpb)+",\"string_dissipation_j\":"+num(wds)+",\"body_dissipation_j\":"+num(wdb)+",\"initial_numerical_energy_j\":"+num(e0)+",\"initial_physical_energy_j\":"+num(m.physicalEnergy(c.initial,V{}))+",\"final_numerical_energy_j\":"+num(s.stringEnergy()+s.bodyEnergy())+",\"final_physical_state_energy_j\":"+num(m.physicalEnergy(end.z,end.v))+",\"reference_final_physical_energy_j\":"+(ref?num(ref->finalPhysicalEnergy):"null")+",\"sav_force_gap\":"+aux.json()+",\"numerical_mass_force_gap\":"+mass.json()+",\"physical_weak_vs_point_gap\":"+(ref?space.json():"null"));return result;}
+ a.add("\"test\":\"trajectory\",\"case\":\""+c.name+"\",\"rate\":"+std::to_string(rate)+",\"lambda\":"+num(lambda)+",\"naive_control\":"+(naive?"true":"false")+","+(ref?result.error.fields():"\"q_error\":null,\"v_error\":null,\"y_error\":null,\"vy_error\":null,\"port_force_error\":null")+",\"joint_balance\":"+num(result.joint)+",\"string_balance\":"+num(result.stringBalance)+",\"body_balance\":"+num(result.bodyBalance)+",\"port_branch_peak_error_n\":"+num(result.forceMismatch)+",\"port_force_peak_n\":"+num(result.forcePeak)+",\"minimum_psi\":"+num(s.minPsi)+",\"minimum_denominator\":"+num(s.minDen)+",\"maximum_denominator\":"+num(s.maxDen)+",\"maximum_phi_drift_j\":"+num(s.maxDrift)+",\"input_work_j\":"+num(win)+",\"string_port_work_j\":"+num(wps)+",\"body_port_work_j\":"+num(wpb)+",\"string_dissipation_j\":"+num(wds)+",\"body_dissipation_j\":"+num(wdb)+",\"initial_numerical_energy_j\":"+num(e0)+",\"initial_physical_energy_j\":"+num(m.physicalEnergy(c.initial,V{}))+",\"final_numerical_energy_j\":"+num(s.stringEnergy()+s.bodyEnergy())+",\"final_physical_state_energy_j\":"+num(m.physicalEnergy(end.z,end.v))+",\"reference_final_physical_energy_j\":"+(ref?num(ref->finalPhysicalEnergy):"null")+",\"sav_force_gap\":"+aux.json()+",\"numerical_mass_force_gap\":"+mass.json()+",\"physical_weak_vs_point_gap\":"+(ref?space.json():"null")+",\"damping_cell_vs_instantaneous_force_gap\":"+dampingTiming.json()+",\"damping_transition_observation\":"+jumpObservation+",\"jump_fraction_of_total_force_squared_error\":"+(c.driven&&ref?num(jumpSquaredForceError/std::max(result.error.f,1e-30)):"null"));return result;}
 
 
 void finiteGuardControls(experiment::Audit&a){Model m(false);for(bool infinity:{false,true})for(const std::string field:{"position","velocity","acceleration","stored_half_velocity","psi","body_force","string_force","point_force","direct_numerical_force","direct_physical_force","accumulated_work"}){Sav s(m,48000,1000);s.initialise(m.pluck(.001));LedgerMonitor monitor(s);auto f=s.step(0,0,false);double bad=infinity?std::numeric_limits<double>::infinity():std::numeric_limits<double>::quiet_NaN();if(field=="position")f.z[0]=bad;else if(field=="velocity")f.v[0]=bad;else if(field=="acceleration")f.acc[0]=bad;else if(field=="stored_half_velocity")s.velocityMinus[0]=bad;else if(field=="psi")s.psi=bad;else if(field=="body_force")f.bodyForce=bad;else if(field=="string_force")f.stringForce=bad;else if(field=="point_force")f.point=bad;else if(field=="direct_numerical_force")f.directNumerical=bad;else if(field=="direct_physical_force")f.physicalAtState=bad;else monitor.win=bad;bool detected=false;try{monitor.add(f,0,0,false);}catch(const std::runtime_error&){detected=true;}a.check(detected,"nonfinite observation control escaped guard");a.add("\"test\":\"nonfinite_guard_sensitivity\",\"field\":\""+field+"\",\"value_kind\":\""+(infinity?"infinity":"nan")+"\",\"detected\":"+(detected?"true":"false"));}}
